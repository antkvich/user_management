version: '3.8'

services:

  app:

    build:
      context: .
      dockerfile: DockerFile

    command: uvicorn src.main:app --host 0.0.0.0

    volumes:
      - .:/app

    ports:
      - "8008:8000"
